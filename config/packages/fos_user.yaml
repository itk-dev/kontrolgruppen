framework:
    csrf_protection: true
    templating:
        engines: ['twig']

fos_user:
    db_driver: orm
    firewall_name: main
    user_class: Kontrolgruppen\CoreBundle\Entity\User
    from_email:
        address: "%env(MAILER_SENDER_ADDRESS)%"
        sender_name: "%env(MAILER_SENDER_NAME)%"
