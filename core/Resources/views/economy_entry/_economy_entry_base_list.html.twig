{% block economy_entry_list %}
    <div class="mb-3 p-3">
        {% for economy_entry in economyEntries %}
            <div class="mb-2">
                <div class="row bg-light">
                    <div class="col-2 pt-2">
                        <span class="text-muted d-block">{{ economy_entry.text }}</span>
                    </div>
                    <div class="col-2 pt-2">
                        <span class="text-muted d-block">{{ economy_entry.date|date('short', 'none') }}</span>
                    </div>
                    <div class="col-2 pt-2">
                        <span class="text-muted d-block">{{ economy_entry.amount|format_currency('DKK') }}</span>
                    </div>
                </div>
            </div>
        {% else %}
            {{ 'common.no_records_found'|trans }}
        {% endfor %}
    </div>
{% endblock %}
