<div class="accordion mb-3" id="tableAccordionBoern">
    <div class="card">
        <div class="card-header bg-primary text-black" id="tableHeadingBoern">
            <h2 class="mb-0">
                <button class="btn btn-link btn-block text-left text-white collapsed" type="button" data-toggle="collapse" data-target="#tableCollapseBoern" aria-expanded="false" aria-controls="tableCollapseBoern">
                    Børn <i class="fa fa-chevron-down float-right" aria-hidden="true"></i>
                </button>
            </h2>
        </div>
        <div id="tableCollapseBoern" class="collapse" aria-labelledby="tableHeadingBoern" data-parent="#tableAccordionBoern">
            <div class="card-body p-0">
                <table class="table mb-0 table-bordered bg-info">
                    <thead class="bg-primary">
                        <tr>
                            <th>Fornavn</th>
                            <th>Efternavn</th>
                            <th>Fødselsdag</th>
                            <th>Forældremyndighed</th>
                        </tr>
                    </thead>
                    <tbody class="bg-light">
                        {# Loop through the data for this table if applicable. Otherwise, statically add rows. #}
                        <tr>
                            {# <td>{{ data.Person.Adresseoplysninger[0].SimpelAdresse.adresselinie1 }}</td> #}
                            {# <td>{{ data.adresse_historik.post_nr_og_by }}</td>
                            <td>{{ data.adresse_historik.kommune }}</td>
                            <td>{{ data.adresse_historik.land }}</td>
                            <td>{{ data.adresse_historik.dato_fra_til }}</td> #}
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>