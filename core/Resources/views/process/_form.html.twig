{{ form_start(form, {'attr': {'class': 'prevent-double-submission'}}) }}
    {{ form_widget(form) }}
<button
    class="btn btn-primary float-left mt-2">{{ (button_label is defined ? (form.vars.full_name ~ '.form.' ~ button_label|lower) : 'process.new.title')|trans }}</button>
{{ form_end(form) }}
