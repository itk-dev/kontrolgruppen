{{ form_start(form, {'attr': {'class': 'prevent-double-submission'}}) }}
    {{ form_widget(form) }}
<button
    class="btn btn-primary float-right">{{ (button_label is defined ? (form.vars.full_name ~ '.form.' ~ button_label|lower) : 'process.new.title')|trans }}</button>

    {{ form_end(form) }}
    <button onclick="history.go(-1);" class="btn btn-light float-left">{{ 'common.actions.back'|trans }}</button>
